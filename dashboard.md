dashboard问题：

生态页面：team_client 不应该作为一个单独生态内容，而应该是用户使用的codeingagent名字。
主页指标： 感叹号+圈 鼠标移动上去应该为当前这个指标的解释。
生产页面里面塞入如下内容是完全不可取的：
“推荐触发率 / 原子采纳率 / canary 晋升率 来自埋点(instrumentation，在代码里插记录点)事件，需累积一段时间才有稳定数字（当前为 0 = 对应事件尚未产生）。”
开发过程的内部细节不应暴露在用户可见面、agent面中。

当前数据内容普遍有bug，你应当在本地的docker ps | grep xskill 容器中测试和部署代码，然后用playwright登陆面板，查看面板信息进行debug。
每个页面截图看一下，思考一下，看看有没有什么异常的内容。
截图思考的过程，分为两种：
1.reviewagent，较为正式，在你呈现给我前必须完成他的意见。
2.你自己查看截图，这个随意，但是一定要自己去看。

reviewagent： 从用户体验出发，发现面板中存量的异常数据，如内容为0、指标不存在可交互的问号解释内容，数据面板为空，生态部分异常等；

dashboard应该包含用户模型统计，用户harnness占比统计；（分别按照轨迹个数、token总量、用户个数等）

我们的指标实现，应当是分析式为主，然后声明式为辅。很多指标用声明式的方式做的话，我们后期版本维护会是一个问题，如果是分析shi，则能够比较好容错。我理解有一些东西必须要用声明的方式去做。，但是当这个方式无效的时候，请你能够回退到分析式的方式来展现面板。



