当用户的代理进行工具调用的时候。其用户册的文件会频繁的发生变化。用户测的消化程序每30秒扫描一次轨迹文件。这就会导致每30秒就会上传一次增量文件到服务器。Yeah.用户测程序在进行增量上传的时候，所以必须要存在一层频率拦截。不然的话，每30秒就会上传服务器触发一次全量流程。这样会导致轨迹原子拆分的时候，原子本身不成体系，或者说根本没有办法拆分。所以我们需要进行限制。我们在用户侧需要记录上一次哈希值的变更更新时间。必须要满足上一次变更时间和当前这一次的时间大于10分钟，或者说是呃对10分钟才可以。这个限制必须要加上。



验收方式由一个agent单独构造数据，快速的变更, 拦截并检查agent提示词中是否包含被切碎的轨迹。
